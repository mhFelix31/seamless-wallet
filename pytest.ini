[pytest]
python_files = test_*.py
testpaths = tests
pythonpath = src

markers =
    unit: fast, pure domain tests
    integration: tests involving DB or external systems
    e2e: full system tests (API + DB), should be calling endpoints
